import { type Activity, type StripMentionsTextOptions } from '@microsoft/teams.api';
import { IActivityContext } from '../contexts';
export declare function stripMentionsText(options?: StripMentionsTextOptions): ({ activity, next }: IActivityContext<Activity, any>) => any;
