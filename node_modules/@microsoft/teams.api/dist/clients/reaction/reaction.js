'use strict';

var teams_common = require('@microsoft/teams.common');
var apiClientSettings = require('../api-client-settings');

class ReactionClient {
  serviceUrl;
  get http() {
    return this._http;
  }
  set http(v) {
    this._http = v;
  }
  _http;
  _apiClientSettings;
  constructor(serviceUrl, options, apiClientSettings$1) {
    this.serviceUrl = serviceUrl;
    if (!options) {
      this._http = new teams_common.Client();
    } else if ("request" in options) {
      this._http = options;
    } else {
      this._http = new teams_common.Client(options);
    }
    this._apiClientSettings = apiClientSettings.mergeApiClientSettings(apiClientSettings$1);
  }
  /**
   * Add a reaction to a message.
   */
  async add(conversationId, activityId, reactionType) {
    const res = await this.http.put(
      `${this.serviceUrl}/v3/conversations/${encodeURIComponent(conversationId)}/activities/${encodeURIComponent(activityId)}/reactions/${encodeURIComponent(reactionType)}`
    );
    return res.data;
  }
  /**
   * Delete a reaction from a message.
   */
  async delete(conversationId, activityId, reactionType) {
    const res = await this.http.delete(
      `${this.serviceUrl}/v3/conversations/${encodeURIComponent(conversationId)}/activities/${encodeURIComponent(activityId)}/reactions/${encodeURIComponent(reactionType)}`
    );
    return res.data;
  }
}

exports.ReactionClient = ReactionClient;
//# sourceMappingURL=reaction.js.map
//# sourceMappingURL=reaction.js.map